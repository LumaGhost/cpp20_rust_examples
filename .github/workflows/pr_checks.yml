name: PR Checks
on:
  pull_request:
    branches:
      - main
jobs:
  build-workflows-windows:
    uses: ./.github/workflows/build_workflows.yml
    with:
      image_name: macos-latest
      platform_name: macos